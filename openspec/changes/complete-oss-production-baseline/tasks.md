## 1. Schema and public contracts

- [x] 1.1 Add `onebox.run/v1` models for S3-compatible storage, failure-domain identity, per-kind encryption policy, trusted credential references, recovery kind, maximum data loss, recurring backup interruption, exact backup/drill schedules, minimum generations/recovery window, restore-proof age, optional restore-staging filesystem, and environment-safe overrides; verify valid intent plus inline-secret, self-target, authored-tool, enablement-restart-as-policy, sparse-drill, unsupported-retention/objective, and target-identity override refusal.
- [x] 1.2 Add the closed `external_services` schema with driver-shaped connections, trusted connection sources, protection owners, optional read-only probes, and run/external name exclusivity; verify with `external_service_valid`, `external_service_ambiguous_owner`, and `external_service_lifecycle_field_refused` fixtures.
- [x] 1.3 Extend canonical output to show authored, default, environment-override, observed, and derived origins for protection, hygiene, service tier, recovery kind, exact service image digest, encryption mode, observed RPO/window, expected interruption, drill capacity state, and prerequisites without secret values; add golden tests covering each origin and redaction.
- [x] 1.4 Define versioned JSON/NDJSON records for protection enablement/disablement, pre-protection and protected service-image patching, backup, replay archive, restore, restore-test, housekeeping, assurance, and service-tier status, including operation identity, stable error code, terminal state, native evidence identity, recovery envelope, and resolving commands; add encoding compatibility and unknown-field tests.
- [x] 1.5 Add stable validation and runtime error codes named by the delta specs, including enable/disable authorization and overdue state, unsafe image reversion, patch-required/unpublished/incompatible/unsupported transitions, patch-during-disable refusal, unsupported major upgrades, retention, drill cadence/capacity, protected rename, encryption evidence, and digest availability, and test every failure is secret-free with a safe resolving command.
- [x] 1.6 Update the checked-in JSON Schema and schema parity/conformance corpus from the Go model, then run the focused schema-generation, parity, canonicalization, and redaction test suites.

## 2. Driver lifecycle foundation

- [x] 2.1 Add a lifecycle-capability interface and per-driver capability record beside the existing runtime driver catalogue, with no default backup implementation; test that unqualified drivers remain runnable but reject protection policies with `backup_driver_unsupported`.
- [x] 2.2 Model recovery kinds, service delivery class, pinned upstream/service/helper provenance and SBOM, supported service-version ranges, exact protected current-to-candidate patch transitions, driver-specific compatibility/continuity probes and rollback limits, native-direct versus artifact ownership, per-kind encryption, native-retention mapping, consistency/topology and enablement-restart preconditions, achievable RPO, credential slots, protected resources, restore/verification operations, and graduation evidence; implement an internal non-graduating test driver that exercises every generic seam while remaining unavailable to schema/status, and validate all records across the eleven production drivers with no default patch transition.
- [x] 2.3 Add `service_image_patch`, `protection_enable`, `protection_disable`, `backup_create`, `backup_prune`, `replay_archive`, `restore_test`, `restore_prepare`, `restore_cutover`, `restore_abort`, `hygiene_run`, and `assurance_check` to the canonical operation graph and structured event registry; test schema dispatch, restricted archive sub-envelopes, scheduled-runner exclusion of image patch and enable/disable, and unsupported-runner refusal.
- [x] 2.4 Implement the service protection lock beneath the application lock so service apply, migration, backup, restore, and credential-affecting work serialize; test bounded contention, cancellation, stale fencing, and `backup_conflict` behavior.
- [x] 2.5 Extend operation journals with deterministic protection step identities, incomplete-resource records, retry classification, helper provenance, and terminal results; test retry after stream disconnect and lookup after client output loss.
- [x] 2.6 Route all new credentials through trusted target-side files and secret slots; run focused tests proving plans, events, journals, errors, manifests, notifications, and structured output redact credentials and database content.

## 3. Active-volume state and runtime generation

- [x] 3.1 Define the sealed, fenced active-volume record containing logical name, selected Docker volume, selection operation, previous selection, and epoch; add encode/decode, tamper, missing-state, and stale-epoch tests.
- [x] 3.2 Seed existing installations by recording the current stable service volume without copying, renaming, recreating, or adopting foreign resources; add migration tests for fresh, existing, missing, and colliding volumes.
- [x] 3.3 Preserve tag rendering only for never-enabled or fully disabled services; derive image/configuration from durable protection lifecycle state rather than policy presence, retain the recorded and manifest-root digests throughout protected refresh and `disable-pending`, permit an exact verified cache during registry outage, and add unprotected-offline, protected-refresh, policy-removal, unsafe-revert, moved-tag, unpublished/unavailable-digest, cached-digest, golden, and local-Docker tests.
- [x] 3.4 Reserve generated service, restore, timer, project, network, volume, and state names during preflight and bind protected service identity once policy or manifests exist; test that foreign collisions and protected renames fail closed and are neither adopted nor mutated.
- [x] 3.5 Generate redacted protection inputs, exact backup/drill schedules, enable/disable configurations and plans, archive hooks, derived images or helpers, lifecycle state paths, native-mapped retention, restore templates, provenance/SBOM references, and artifact digests, then bind them into executable plans; derive desired artifacts from current project intent plus the durable last-effective projection during `disable-pending`, add matching-retained-versus-real-drift tests for every artifact class, and prove snapshot-only drivers receive no replay helper.
- [x] 3.6 Add inspection and authorized removal for Onebox-owned protection units only after prerequisite disablement, preserving remote backups, manifests, manifest images, live data, previous volumes, and foreign units; verify disable, pending-disable, and destroy regressions.
- [x] 3.7 Implement the fenced `never-enabled`/`enabled`/`disable-pending`/`disabled` state machine and crash-resumable `protection_disable` plan; retain required image/hooks plus the last effective base-backup, archive, prune, target, and retention contract while pending, stop drills, record age and a 24-hour action deadline, allow unrelated applies against the retained projection, refuse refresh with `service_image_patch_disable_pending`, and refuse unsafe reversion; add missing-approval, continued-retention, retained-not-drift, unrelated-apply, refresh-refusal, stopped-drill, overdue-state, in-flight operation, phase-crash, retry, rollback, safe-removal, and no-remote-deletion tests with the non-graduating driver.

## 4. Canonical scheduled runner

- [x] 4.1 Build the short-lived `ob-scheduled-runner` from the same canonical Go service used by the CLI, with no listener and an allowlist containing only scheduled operation schemas; add command-surface tests proving it cannot plan deployments or mint approvals.
- [x] 4.2 Define and seal the scheduled-operation envelope with protocol range, app/environment identity, operation kind, timing policy, artifact digests, state binding, and secret-file references; declare the runner's accepted CLI/envelope ranges and test tamper, older-CLI/newer-runner, newer-CLI/older-runner, and stale-envelope refusal.
- [x] 4.3 Install the runner and envelope with digest and publication-provenance verification plus least-privilege permissions under the Onebox host layout; add target-layout tests for upgrade, replay, ownership, mode enforcement, and authorized destroy removing only unreferenced executable artifacts.
- [x] 4.4 Generate, inspect, enable, disable, and remove exact systemd service/timer units for base backup, replay archive, restore drill, housekeeping, and assurance; implement the `disable-pending` transition that keeps only required base/archive/prune units and stops drill units, and verify reboot persistence and idempotent convergence in the systemd integration suite.
- [x] 4.5 Make scheduled executions use the canonical lock, fence, journal, retry identity, helper-provenance, cancellation, and redaction paths; add crash, cancellation, lock-contention, and disconnected-client fault tests.

## 5. Repository, manifest, and backup core

- [x] 5.1 Implement the closed S3-compatible target adapter for endpoint, bucket, prefix, region, TLS policy, per-kind encryption mode/evidence, failure-domain identity, and trusted mode-0600 credential file, plus a test-only local repository that explicitly reports non-off-host protection; add validation, self-target, alias, unproven-encryption, and target-probe tests.
- [ ] 5.2 Define minimal repository interfaces for native-direct engines and encrypted Restic artifact storage; add contract tests for identity, health, retry, per-kind encryption, native retention ownership, and read-only inspection using the test driver before production-driver gates.
- [ ] 5.3 Package and verify the pinned Restic artifact helper, enforce its repository/runner compatibility, and prove only artifact-producing driver contracts can select it; add incompatible-helper, digest-mismatch, and accidental-database-engine selection tests.
- [ ] 5.4 Implement streaming artifact ingestion without a plaintext intermediate and restricted operation-scoped staging for native APIs that require files; test successful cleanup, residual reporting, capacity bounds, cancellation, target loss, helper crash, and retry/resume.
- [ ] 5.5 Persist the sealed secret-free protection manifest with app, environment, immutable service image digest, driver/method/helper provenance, recovery kind, encryption mode, target, protected resources, native repository identity, optional base/replay range, and artifact digest/size only when artifacts exist; add tamper, cross-service ownership, and range tests.
- [ ] 5.6 Implement manual `backup create`, `backup list`, `backup inspect`, replay inspection, and protection `status` through the canonical CLI with versioned JSON/NDJSON; add golden tests for human output, structured output, idempotent retry, and read-only behavior.
- [ ] 5.7 Implement and validate each capability record's mapping from minimum generations/recovery window to pgBackRest, PBM, ClickHouse-chain, artifact, or snapshot retention semantics, acting only after a newly recoverable point exists and never deleting native-repository objects directly; test unsupported mapping, failed-new-backup preservation, replay gaps, ancestors, foreign content, ambiguity refusal, and policy removal without remote deletion.
- [ ] 5.8 Add target diagnostics and stable classifications for unreachable, unauthorized, non-independent, locked, corrupt, incomplete, incompatible, replay-gap, encryption, and capacity failures; verify each result is secret-free and names a read-only diagnosis or recovery command.

## 6. Restore, cutover, and restore drills

- [ ] 6.1 Implement recovery-point selection and compatibility validation bound to manifest, native repository state, base/replay continuity, driver, recorded immutable service image digest, active-volume state, interruption contract, driver-bounded second-copy footprint, and effective staging filesystem; add stale, corrupt, discontinuous, wrong-service, unavailable-digest, incompatible-version, and insufficient-disk tests.
- [ ] 6.2 Implement `restore prepare` to create an operation-named isolated volume, materialize the selected artifact, native recovery point, or cold generation through its exact driver path, start the recorded-digest temporary service with protected identity prerequisites, and run integrity verification without changing live selection; add artifact/native-direct local-Docker and cancellation tests.
- [ ] 6.3 Persist staged-restore evidence and cleanup state so failed or incomplete temporary resources remain inspectable and require an explicit resolving command; test runner crash and safe repeated cleanup.
- [ ] 6.4 Implement `restore test` through the same applicable materialize, startup, and driver-verification path as live restore for artifact and native-direct contracts, recording selected recovery identity, runner/digest, method, result, timestamps, and expiry; test checksum-only refusal plus `drill_deferred_capacity` without proof extension or false corruption.
- [ ] 6.5 Generate a state-bound cutover plan and require a fresh strong approval delivered independently of model-authored text; test absent, expired, mismatched, and stale-state approval refusal.
- [ ] 6.6 Implement restore cutover with durable phase markers: stop live service, commit fenced selection, render/start/verify the restored service, and retain the former volume as the previous recovery choice; add injected-crash tests at every phase boundary.
- [ ] 6.7 Implement `ob resume` and `ob abort` recovery choices that preserve both volumes and never treat force as deletion permission; test safe rollback when possible and honest manual-recovery stop when safety cannot be proven.
- [ ] 6.8 Implement backup, replay archive, and restore structured event streams and terminal records, then test retry after connection loss returns or resumes the same operation instead of starting an unrelated mutation.

## 7. PostgreSQL qualification gate

- [ ] 7.1 Implement the closed PostgreSQL capability release record mapping an authored selector plus exact upstream patch/base digest to pgBackRest version, derived digest, publication time, and support state; publish the current supported patch at qualification and forward mappings only, and test no historical-backfill claim, no implicit patch, moved tags, unpublished mappings, verified cache, and overdue status.
- [ ] 7.2 Add the repository-owned scheduled and manually dispatchable GitHub Actions publication pipeline: poll upstream at least daily, trigger on a new supported digest, build twice for reproducibility, run compatibility/restore smoke tests, emit SBOM, sign or transparency-publish provenance, push the image, and atomically publish the qualified mapping; add failure-preserves-prior-mapping and observed-digest-to-qualified-mapping E2E evidence for the 72-hour security/seven-day routine targets.
- [ ] 7.3 Implement the state-bound same-major `ob service apply --refresh-image` onboarding plan for existing PostgreSQL bases outside the published window, resolving an exact qualified upstream digest, preserving the volume and rollback identity, restarting and verifying without enabling protection, and returning the subsequent protection command; add stale-plan, missing-approval, failed-patch, rollback, success, and no-major-upgrade tests.
- [ ] 7.4 Implement the state-bound, strongly approved protected same-major `ob service apply --refresh-image` plan, binding old/new PostgreSQL and pgBackRest digests, volume/configuration identity, stanza/repository identity, a fresh pre-patch recovery point, and WAL position; keep archive mode, command, credentials, and schedules effective across one restart, verify health/stanza/archive round trip/WAL continuity, retain prior manifest-root digests, and add compatibility-refusal, stale-plan, missing-approval, success, safe rollback, failed-verification, no-chain-break, and no-major-upgrade tests.
- [ ] 7.5 Implement least-privilege PostgreSQL/pgBackRest runtime and repository configuration for each published mapping, including stanza, S3-compatible settings, repository encryption, resource limits, archive/restore commands inside the derived image, and digest-bound rendering; add catalogue, compatibility, redaction, and configuration tests.
- [ ] 7.6 Implement the state-bound archive-mode enablement and restart plan with expected outage, rollback, strong-approval refusal, post-restart health, and re-observation of archive settings/configuration identity at backup and status time; add missing-approval, failed-restart, rollback, drift, and successful-reenable tests.
- [ ] 7.7 Implement PostgreSQL disablement that keeps the derived image/archive path effective until it stops new base work, reconciles archive state, disables archive mode, restarts and verifies WAL recycling, then removes hooks and optionally returns to tag rendering; while pending keep base/archive/prune schedules under the last target/retention contract and stop drills; add missing-approval, pending-age/deadline, archive-command dependency, retention-bound growth, full-volume prevention, phase-crash, rollback, and retained-manifest-image tests.
- [ ] 7.8 Implement pgBackRest stanza creation/check, full/differential/incremental backup, WAL archive push/get integration, continuity evidence, retention, and recovery-point listing; add native repository tests for gaps, duplicates, target loss, and archive-command failure.
- [ ] 7.9 Implement isolated pgBackRest restore and PITR to an exact-compatible PostgreSQL service, verifying startup, timeline, target recovery point, catalogue readability, object counts, and declared checks; add successful, corrupt base, missing WAL, wrong timeline, and incompatible-version tests.
- [ ] 7.10 Run the PostgreSQL qualification matrix for patch-then-protect onboarding, protected same-major derived-image patching with one restart and uninterrupted WAL continuity, approved enable/disable restarts, bounded pending-disable schedules, effective-prerequisite drift, base backup, WAL archiving, PITR boundaries, exact-derived-image isolated restore, fresh pre-patch recovery, restore drill, live cutover, corruption, cancellation, runner loss, target outage, native retention safety, direct pgBackRest recovery without Onebox, and injected crash recovery in local-Docker E2E.
- [ ] 7.11 Only after task 7.10 and its direct-recovery runbook pass, enable PostgreSQL `Managed`/`pitr` graduation for the exact tested matrix and add evidence tests for current, patch-available, image-publication-overdue-but-valid, stale, discontinuous, drifted, disable-pending/overdue, failed, missing, and recovered protection.
- [ ] 7.12 Only after task 7.11 passes, document pre-protection and protected refresh-image paths, forward-only mapping window, publication pipeline/SLO, qualified PostgreSQL mappings, pending schedules and safe disablement, measured recovery envelope, direct pgBackRest recovery, and all configurations that remain `Run`.

## 8. MySQL qualification gate

- [ ] 8.1 Implement the exact MySQL/Percona XtraBackup/server-edition/storage-engine compatibility matrix with a pinned helper whose UID/GID, exact data-volume mounts, socket/network scope, credentials, privileges, locks, and resources are contract-bound; add supported-version, mount isolation, mixed-engine, incompatible-edition, unreadable-metadata, and conflict tests.
- [ ] 8.2 Implement full/incremental physical backup, prepare, artifact encryption/upload, base-position capture, closed-binary-log rotation/archive, continuity tracking, and retention; add helper integration tests for broken chains, partial streams, duplicate logs, and target loss.
- [ ] 8.3 Implement empty exact-compatible MySQL restore plus binary-log replay to a selected point, verifying startup, server identity, replay position, table/catalogue readability, counts, and declared checks; add successful, corrupt, gap, and cross-version refusal tests.
- [ ] 8.4 Qualify protected non-major MySQL refresh transitions against the exact server-edition/storage-engine/XtraBackup matrix; bind old/new service and helper digests, a fresh recovery point and binlog position, keep binary logging/archive schedules effective across restart, verify helper access and binlog continuity, retain prior manifest roots, and add unsupported-transition, pending-disable, rollback, and no-gap tests.
- [ ] 8.5 Run the MySQL qualification matrix for protected image patching, physical backup, binlog archive, PITR boundaries, isolated restore, restore drill, live cutover, corrupt artifact, cancellation, runner loss, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 8.6 Only after task 8.5 and a tested direct XtraBackup/binlog recovery runbook pass, enable MySQL `Managed`/`pitr` graduation for the exact tested envelope and add tier-evidence tests; leave other MySQL versions or engines `Run` without logical fallback, and keep unqualified patch transitions unavailable without demoting current recovery evidence.
- [ ] 8.7 Only after task 8.6 passes, document the qualified MySQL and protected-patch matrices, observed RPO, direct recovery dependencies, and unsupported configurations.

## 9. MariaDB qualification gate

- [ ] 9.1 Implement the separate MariaDB/`mariadb-backup`/storage-engine matrix with a pinned contract-mounted helper plus driver-owned `log_bin` configuration and a state-bound strongly approved one-time enablement restart; verify effective binary logging and rollback, and add version, engine, mount isolation, missing-approval, helper-mismatch, and active-condition tests.
- [ ] 9.2 Implement full/incremental physical backup, prepare, artifact encryption/upload, binlog-coordinate capture, closed-binlog archive, continuity tracking, and retention; add native helper tests for broken increments, partial streams, binlog gaps, and target loss.
- [ ] 9.3 Implement empty exact-compatible MariaDB restore plus binlog replay to a selected point, verifying startup, server identity, replay position, catalogues, counts, and declared checks; add successful, corrupt, gap, and cross-version refusal tests.
- [ ] 9.4 Qualify protected non-major MariaDB refresh transitions against the exact server/storage-engine/`mariadb-backup` matrix; bind old/new service/helper digests, fresh recovery point and binlog coordinate, keep `log_bin` and archive schedules effective, verify helper access and binlog continuity after restart, retain prior manifest roots, and add unsupported-transition, pending-disable, rollback, and no-gap tests.
- [ ] 9.5 Run the MariaDB qualification matrix for protected image patching, physical backup, binlog archive, PITR boundaries, isolated restore, restore drill, live cutover, corrupt artifact, cancellation, runner loss, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 9.6 Only after task 9.5 and a tested direct MariaDB Backup/binlog recovery runbook pass, enable MariaDB `Managed`/`pitr` graduation for the exact tested envelope and add tier-evidence tests independently of MySQL.
- [ ] 9.7 Only after task 9.6 passes, document qualified MariaDB versions and protected-patch transitions, observed RPO, direct recovery dependencies, and configurations that remain `Run`.

## 10. MongoDB replica-set runtime and qualification gate

- [ ] 10.1 Change newly created MongoDB services to a single-node replica set with generated internal keyfile, idempotent initialization, PRIMARY health, durable config, and `replicaSet` connection projection; add fresh-install, restart, retry, and redaction E2E tests.
- [ ] 10.2 Add an explicit state-bound conversion plan for existing standalone MongoDB volumes that preserves the original data and refuses automatic or ambiguous adoption; add successful conversion, stale plan, failed startup, and recovery tests.
- [ ] 10.3 Implement the MongoDB/PBM compatibility matrix and least-privilege pinned PBM helper sidecar, refusing protection when replica-set, PRIMARY, helper-health, or storage evidence is absent; add standalone, election, incompatible-tool, sidecar-loss, and healthy-replica-set tests.
- [ ] 10.4 Implement PBM logical base backup, oplog slicing, S3-compatible storage, continuity/status translation, retention, and recovery-point listing; add gaps, target loss, interrupted backup, election, and stale-sidecar integration tests.
- [ ] 10.5 Implement isolated PBM restore and PITR to an exact-compatible MongoDB replica set, verifying startup, PRIMARY, target timestamp, catalogues, collection/index counts, users/roles scope, and declared checks; add successful, corrupt-base, missing-oplog, and incompatible-version tests.
- [ ] 10.6 Qualify protected non-major MongoDB refresh transitions against the exact mongod/PBM matrix; bind old/new service and sidecar digests, replica-set identity, fresh recovery point and oplog marker, keep PBM and oplog capture effective, verify PRIMARY/sidecar/repository health and oplog continuity after restart, retain prior manifest roots, and add election, unsupported-transition, pending-disable, rollback, and no-gap tests.
- [ ] 10.7 Run the MongoDB qualification matrix for protected image/sidecar patching, base backup, oplog PITR boundaries, isolated restore, restore drill, live cutover, standalone refusal, corruption, cancellation, runner/sidecar loss, election, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 10.8 Only after task 10.7 and a tested direct PBM recovery/ownership-handback runbook pass, enable and document MongoDB `Managed`/`pitr` for the exact single-node replica-set and protected-patch envelope; keep standalone, incompatible, and unhealthy-helper configurations `Run`, and keep unqualified patch transitions unavailable without demoting current recovery evidence.

## 11. ClickHouse qualification gate

- [ ] 11.1 Implement the ClickHouse native-backup matrix and digest-bound named S3 collection with secret references, explicit archive-password or observed SSE mode, privileges, concurrency/resource budgets, and a strongly approved one-time restart when configuration activation requires it; add rendering, encryption-refusal, missing-approval, query-log redaction, and unsupported-version tests.
- [ ] 11.2 Implement native full/incremental `BACKUP` orchestration, asynchronous status translation from `system.backups`, manifest binding, base-chain retention, and cancellation/retry reconciliation; add successful, failed, duplicate-ID, missing-base, and target-outage tests.
- [ ] 11.3 Implement native `RESTORE` into an empty exact-compatible ClickHouse service and verify databases, tables, parts, counts, access scope, and declared queries; add corrupt, wrong-password, incomplete-chain, and incompatible-version tests.
- [ ] 11.4 Qualify protected non-major ClickHouse refresh transitions against the native-backup/configuration matrix; bind old/new service digests, named-collection identity and fresh recoverable chain head, preserve protection configuration, verify startup, `system.backups`, repository readability and declared queries, retain prior manifest roots, and add unsupported-transition, pending-disable, rollback, and chain-readability tests.
- [ ] 11.5 Run the ClickHouse qualification matrix for protected image patching, full/incremental backup, isolated restore, restore drill, live cutover, cancellation, runner loss, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 11.6 Only after task 11.5 and a tested direct ClickHouse recovery runbook pass, enable and document ClickHouse `Managed`/`snapshot` plus protected patching for the exact tested encryption envelope; leave other versions/configurations `Run` and unqualified patch transitions unavailable without demoting current recovery evidence.

## 12. Redis qualification gate

- [ ] 12.1 Implement separate Redis matrices for the qualified sealed BASE/INCR/manifest backup contract and the older-version immutable-RDB fallback, preflighting memory, modules, authentication, AOF/RDB state, and supported `preload-file`/rewrite behavior; add supported-path, insufficient-memory, busy-persistence, and module tests while leaving unmatched versions `Run`.
- [ ] 12.2 For the modern matrix create, list, seal, and ingest the native backup set with its seal recovery point; for the fallback request a complete immutable RDB and ingest it through encrypted artifact storage, recording server/persistence identity and counts; add incomplete-set/RDB, target-loss, cancellation, and retry tests.
- [ ] 12.3 Restore modern sets through the qualified manifest preload path; restore fallback RDBs with a first boot that cannot load AOF, verify databases/keys/TTLs, then enable AOF and require a successful rewrite before eligibility; add empty-AOF-precedence, existing-appenddir, corrupt, incompatible-version, and module-mismatch tests.
- [ ] 12.4 Qualify protected non-major Redis refresh transitions independently for sealed-set and RDB matrices; bind old/new service digests, modules and persistence identity, require a fresh qualified recovery point, verify restart, key/TTL health and post-patch backup compatibility, retain prior manifest roots, and add module/persistence mismatch, unsupported-transition, pending-disable, rollback, and recovery-readability tests.
- [ ] 12.5 Run the Redis qualification matrix for protected image patching, snapshot backup, isolated restore, restore drill, live cutover, memory pressure, cancellation, runner loss, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 12.6 Only after task 12.5 and direct sealed-backup/RDB recovery runbooks pass, enable and document Redis `Managed`/`snapshot` and protected patching for each exact matrix, its measured seal or RDB recovery point and memory requirement, while making clear that local AOF durability is not PITR evidence.

## 13. Valkey qualification gate

- [ ] 13.1 Implement a Valkey-specific version/persistence compatibility matrix and remote-RDB preflight without inheriting Redis file compatibility outside explicitly tested versions; add server-identity, memory, busy-persistence, and cross-product refusal tests.
- [ ] 13.2 Create and ingest a complete immutable Valkey RDB through encrypted artifact storage, recording persistence state, key counts, digest, and snapshot time; add partial-transfer, target-loss, cancellation, and retry tests.
- [ ] 13.3 Restore into an empty exact-compatible Valkey service with a first boot that cannot load AOF, verify RDB load, server identity, databases, keys and TTLs, then enable AOF and require a successful rewrite before eligibility; add empty-AOF precedence, existing-appenddir, corrupt-RDB, Redis-produced-RDB refusal, and incompatible-version tests.
- [ ] 13.4 Qualify protected non-major Valkey refresh transitions against the exact persistence matrix; bind old/new service digests and persistence identity, require a fresh immutable RDB recovery point, verify restart, key/TTL health and post-patch RDB compatibility, retain prior manifest roots, and add cross-product, unsupported-transition, pending-disable, rollback, and recovery-readability tests.
- [ ] 13.5 Run the Valkey qualification matrix for protected image patching, snapshot backup, isolated restore, restore drill, live cutover, memory pressure, cancellation, runner loss, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 13.6 Only after task 13.5 and a tested direct Valkey RDB/AOF recovery runbook pass, enable and document Valkey `Managed`/`snapshot` plus protected patching for the exact tested matrix, independently of Redis.

## 14. RabbitMQ cold qualification gate

- [ ] 14.1 Generate and pin a stable `RABBITMQ_NODENAME`, record it and Erlang-cookie references as protected identity, implement version/storage/queue discovery plus topology-only and complete-cold reporting, and test rename refusal, restart stability, restore identity mismatch, and definitions-only non-graduation.
- [ ] 14.2 Implement live definitions export and verification as non-graduating topology evidence; add users/vhosts/policies/queues/exchanges/bindings round-trip tests with credential redaction.
- [ ] 14.3 Implement a strongly approved stopped-node backup that drains/stops the broker, archives the complete data directory and required generated identity material through encrypted artifact storage, restarts and verifies the live broker, and records interruption; add stop failure, upload failure, restart failure, and abort tests.
- [ ] 14.4 Restore the cold generation into an empty exact-version, exact-node-name isolated broker and verify definitions, queue types, durable messages, streams, and declared checks; add live-copy refusal, corrupt-store, wrong-cookie, wrong-node-name, and incompatible-version tests.
- [ ] 14.5 Qualify protected non-major RabbitMQ refresh transitions only where node-store format and rollback are proven compatible; bind old/new service digests, node/cookie identity and a fresh complete cold recovery point, preserve generated identity, verify broker/queue/stream/message health after restart, retain prior manifest roots, and add store-migration refusal, unsupported-transition, pending-disable, rollback, and recovery tests.
- [ ] 14.6 Run the RabbitMQ qualification matrix for protected image patching, cold backup, exact-identity isolated restore, restore drill, approved interruption, cancellation, runner loss, target outage, retention safety, crash recovery, and direct node-store recovery without Onebox; only after that runbook passes enable and document `Managed`/`cold` plus protected patching with measured downtime, leaving topology-only protection `Run` and unqualified patch transitions unavailable without demoting current recovery evidence.

## 15. MinIO cold qualification gate

- [ ] 15.1 Implement the exact-version single-node MinIO capability record with data/configuration identity, target capacity, authored interruption, and failure-domain preflight; refuse a target resolving to the protected host or MinIO endpoint and add alias, DNS, credential, capacity, and `backup_target_not_independent` tests.
- [ ] 15.2 Implement a strongly approved stopped-service backup that stops the exact MinIO service, streams its data and required configuration through encrypted Restic artifact storage, restarts and verifies the live service, and records interruption; add stop, upload, restart, cancellation, and target-loss tests.
- [ ] 15.3 Restore a cold artifact into an empty exact-compatible single-node MinIO service and verify startup, bucket configuration, object checksums, declared identity scope, and application reads; add corrupt-artifact, missing-configuration, wrong-identity, and incompatible-version tests.
- [ ] 15.4 Exercise the same cold path through isolated restore drill and live cutover, preserve the previous volume, and publish a direct Restic recovery/ownership-handback runbook; add failed-drill, stale-proof, runner-loss, and cutover-crash tests.
- [ ] 15.5 Qualify protected non-major MinIO maintenance transitions against exact data/configuration compatibility; bind old/new service digests and a fresh cold recovery point, preserve identity and configuration, verify service/object health after restart, retain prior manifest roots, and add unsupported-transition, pending-disable, rollback, and recovery tests.
- [ ] 15.6 Run the MinIO qualification matrix for protected image patching, approved cold backup interruption, exact-version isolated restore, restore drill, live cutover, cancellation, runner loss, target outage, retention safety, and crash recovery; only after it and the direct recovery runbook pass enable and document `Managed`/`cold`, leaving live-copy and unqualified configurations `Run`.

## 16. Meilisearch qualification gate

- [ ] 16.1 Implement the Meilisearch version matrix and native snapshot task/status integration with artifact staging, encryption, resource limits, and master-key secret references; add supported-version, task-failure, capacity, and redaction tests.
- [ ] 16.2 Create exact-version periodic snapshots and separate portable pre-upgrade dumps, manifesting their different compatibility and purpose; add task retry, partial artifact, target-loss, and snapshot-versus-dump classification tests.
- [ ] 16.3 Restore into an empty compatible Meilisearch service and verify task completion, indexes, settings, synonyms, document counts, and declared searches; add corrupt, newer-dump-to-older-version, wrong-key, and incompatible-snapshot tests.
- [ ] 16.4 Qualify protected non-major Meilisearch refresh transitions against snapshot/data-format compatibility; bind old/new service digests, master-key identity, a fresh exact snapshot and portable pre-patch dump when required, verify task/index/settings/document/search health after restart, retain prior manifest roots, and add data-migration, unsupported-transition, pending-disable, rollback, and prior-snapshot readability tests.
- [ ] 16.5 Run the Meilisearch qualification matrix for protected image patching, snapshot backup, isolated restore, restore drill, live cutover, cancellation, runner loss, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 16.6 Only after task 16.5 and a tested direct Meilisearch snapshot recovery runbook pass, enable and document Meilisearch `Managed`/`snapshot` plus protected patching for qualified transitions; keep portable dumps as upgrade evidence rather than backup-tier evidence.

## 17. NATS JetStream qualification gate

- [ ] 17.1 Add generated least-privilege NATS account credentials, workload connection projection, and a digest-pinned external NATS CLI health probe for new runtimes; add fresh-install, restart, missing-probe, permissions, connection, and redaction tests.
- [ ] 17.2 Implement the state-bound strongly approved conversion for existing unauthenticated NATS runtimes, naming connection interruption and affected workloads, updating server and workload credentials in one fenced operation, redeploying dependants, verifying authenticated reconnection/stream health, and retaining bounded rollback; add missing-approval, disconnect, partial-redeploy, failed-health, rollback, and retry tests.
- [ ] 17.3 Implement the NATS version, account, file-backed stream, credential, helper-health, and consumer-state compatibility matrix, refusing memory-only streams and unconverted legacy runtimes with exact resolving commands; add mixed-storage, permissions, unhealthy-probe, and unsupported-version tests.
- [ ] 17.4 Use the pinned NATS CLI to create checked account backups containing stream configuration/state, durable consumers, and messages, then ingest them through encrypted artifact storage; add partial stream, critical-warning, target-loss, cancellation, and retry tests.
- [ ] 17.5 Restore into an empty exact-compatible NATS service/account and verify streams, subjects, message counts/content samples, durable consumer configuration/state, and declared checks; add existing-stream, corrupt-snapshot, and incompatible-version tests.
- [ ] 17.6 Qualify protected non-major NATS refresh transitions against the server/account/file-store/CLI matrix; bind old/new service and helper digests, credential identity, fresh account recovery point and stream/consumer sequence markers, verify authenticated health and sequence continuity after restart, retain prior manifest roots, and add helper mismatch, unsupported-transition, pending-disable, rollback, and no-loss tests.
- [ ] 17.7 Run the NATS qualification matrix for protected image/helper patching, authenticated account backup, legacy conversion interruption/rollback, helper health, isolated restore, restore drill, live cutover, cancellation, runner loss, target outage, retention safety, and crash recovery in local-Docker E2E.
- [ ] 17.8 Only after task 17.7 and a tested authenticated direct NATS account recovery runbook pass, enable and document NATS JetStream `Managed`/`snapshot` plus protected patching for file-backed streams; keep memory-only, unauthenticated legacy, unhealthy-probe, or incomplete accounts `Run`, and keep unqualified patch transitions unavailable without demoting current recovery evidence.

## 18. Host operational hygiene

- [ ] 18.1 Apply the documented Docker `local` logging defaults of 20 MiB and five files to every generated workload, service, helper, and proxy container while preserving authored policies; add rendering and effective-origin golden tests.
- [ ] 18.2 Verify effective runtime log policies and report unsupported custom drivers as externally managed instead of protected; add status/doctor tests for default, authored, drifted, and unverifiable policies.
- [ ] 18.3 Build the image ownership and reachability graph from current and retained releases, services, proxy, scheduled jobs, helpers, every service-image digest referenced by a retained protection manifest, and active/incomplete restore state; add graph and post-service-patch restore tests proving every rollback and recovery root is retained.
- [ ] 18.4 Implement scoped pruning of only proven Onebox-owned unreachable images, recomputing reachability after cancellation or retry and never calling system-wide prune; add foreign-image, retained-release, interrupted-prune, and idempotency E2E tests.
- [ ] 18.5 Probe headroom for each distinct filesystem backing the app base, Docker data root, backup staging, and default or authored restore/drill staging; require each driver to estimate and cap its second-copy footprint and add a host-wide atomic per-filesystem admission/reservation ledger across all apps and services; test boundaries, simultaneous admissions, distinct filesystems, release after crash/cleanup, unavailable probes, and estimate overruns.
- [ ] 18.6 Gate space-increasing deploy, backup, and restore operations on critical pressure and aggregate staging reservations while keeping reads, cleanup planning, backup listing, and recovery available; serialize or report `drill_deferred_capacity` before materialization without extending proof expiry, and test shared-slot contention, deferred recovery after capacity release, `disk_pressure_critical`, and every escape path.
- [ ] 18.7 Implement housekeeping plan/run/status and its host schedule through the canonical runner, with versioned structured reclaimed-byte, deleted-image, and preserved-root results; add reboot, disable, retry, cancellation, and redaction tests.

## 19. Local continuous assurance

- [ ] 19.1 Implement the read-only assurance check graph for workloads, run services, external probes, disk, certificate runway, backup freshness, replay continuity, restore-proof freshness, and generated units/helpers; for `disable-pending`, continue service/unit/deadline/storage checks but replace backup and drill freshness with the pending action state; add a capability test proving no converge or repair operation is reachable.
- [ ] 19.2 Persist assurance evidence atomically with start, completion, check set, runner provenance, observations, expiry, and per-check state; add tests distinguishing current, stale, incomplete, never-run, and failed evidence.
- [ ] 19.3 Install assurance and twice-weekly restore-drill windows through the canonical runner, derive stable UTC offsets from the canonical application identity, environment, declared service name, and driver so defaults are spread across applications and services, validate the full window plus host admission delay and driver runtime inside proof age, and reconcile interrupted checks as fresh reads; add deterministic-schedule, cross-application same-name spread, multi-service spread, reboot, cancellation, runner-upgrade, sparse-schedule refusal, and partial-check fault tests.
- [ ] 19.4 Add transition and authored-or-default-24-hour reminder notifications through the existing webhook contract, with notification delivery state separate from observed health; emit one pending-disable transition, suppress its stale-backup/drill transitions, and notify/remind on `protection_disablement_overdue`; test transition deduplication, reminder cadence, webhook failure, and secret redaction.
- [ ] 19.5 Extend status and doctor with versioned assurance records, stable check codes, evidence identifiers, expiry, and resolving commands while keeping reads side-effect free; include pending-disable request time, deadline, elapsed age, continued schedule/storage contract, and exact resolving command; add human/JSON golden tests and mutation guards.

## 20. External-service connections

- [ ] 20.1 Normalize external-service declarations into canonical driver-shaped connection metadata and the derived `External` tier; add canonicalization and run/external ownership-conflict tests.
- [ ] 20.2 Resolve workload `needs` entries across run and external services, projecting only requested parts from trusted staged secret files and generating no external service runtime; add runtime golden and plaintext-leak tests.
- [ ] 20.3 Implement optional read-only external health probes with bounded age and plan binding; add healthy, permission-denied, unreachable, changed-after-plan, and `external_service_state_stale` tests proving no corrective mutation occurs.
- [ ] 20.4 Refuse apply, backup, restore, upgrade, rotation, and destroy against external dependencies with `external_service_not_owned`, preserving the declared owner in secret-free output; add command matrix tests.
- [ ] 20.5 Validate fresh plan-bound external backup evidence or an independently authorized override for migration policy without creating, storing, restoring, or claiming provider backups; add stale, mismatched, missing, and accepted receipt tests.

## 21. Official CI image delivery

- [ ] 21.1 Add a reusable GitHub Actions workflow using pinned upstream actions to build selected build-sourced workloads, push them, confirm registry digests, and emit the versioned workload-to-digest result; validate the workflow with multi-workload and digest-resolution failure fixtures.
- [ ] 21.2 Make the workflow call only shipped CLI validation, planning, approval validation, deploy, status, and resume commands, and add a repository check that rejects duplicated render, drift, lock, fencing, rollback, or recovery logic in workflow scripts.
- [ ] 21.3 Separate plan-only output from deployment so a missing independently delivered matching approval always stops before deploy and the workflow never mints approval; add absent, mismatched, expired, and accepted approval tests.
- [ ] 21.4 Persist operation identity before deployment, inspect it after runner loss, and reuse a plan only while its state binding remains valid; add retry fixtures for in-progress, terminal, expired-plan, and changed-state cases.
- [ ] 21.5 Audit CI artifacts and logs so only redacted validation, digest mappings, plans, and structured results are uploaded while registry, SSH, backup, application, and approval-delivery secrets remain in the CI secret facility; add automated secret-canary tests.
- [ ] 21.6 Publish an equivalent documented shell sequence that performs the same build/push/digest/plan/approved-deploy handoff without becoming a second deployment implementation; verify every documented command in the CLI docs test harness.

## 22. Integration, documentation, and release evidence

- [ ] 22.1 Add an end-to-end service-tier suite proving `Run`, `Managed`, and `External` plus `snapshot`, `pitr`, and `cold` recovery envelopes are derived from observed evidence; cover stale backup, replay gaps, stale/deferred drill, aggregate drill admission, enablement-prerequisite drift, `disable-pending` and overdue action state without false freshness alerts, unsafe image reversion, overdue derived-image rebuilds that retain otherwise valid `Managed` evidence, same-major patch-required onboarding, protected patch availability/completion/unsupported transitions without recovery-tier demotion across all delivery classes, patch-during-disable refusal, unhealthy external probes, real versus retained-pending drift, unit drift, protection-scoped/unpublished/unavailable image digests, unproven encryption, missing resources, and recovery to current evidence.
- [ ] 22.2 Add a cross-capability fault suite covering disconnect, retry, cancellation, stale plans, partial evidence, corrupt state, disk pressure, simultaneous drill admission, lock contention, runner/helper crash, driver-specific protected-patch verification/rollback and continuity across all eleven services, protection-disable phase crashes, retained-artifact projection and pending schedule transitions, PostgreSQL WAL-fill prevention during disablement, derived-image publication failure, archive gaps, NATS conversion interruption, and target outage without data deletion or secret disclosure.
- [ ] 22.3 Document protection-scoped digest resolution, delivery-class and driver-qualified protected patch transitions, PostgreSQL selector/image mapping and forward-only publication pipeline/SLO, explicit pre-protection and protected refresh-image paths, per-driver compatibility/continuity/refusal behavior, derived image provenance, enablement restarts and live prerequisite observation, pending artifact projection and schedule/storage behavior, safe stateful disablement, the distinction between recovery tier and patch currency, canonical drill schedule identity, NATS conversion interruption, per-kind encryption, native retention mappings, aggregate drill admission and staging setup, failure-domain independence, MinIO's stopped-service cold contract, recovery objectives/schedules, deferred drills, manual recovery, tier/envelope, hygiene, assurance, external ownership, and CI handoff in README, product, CLI, and schema references.
- [ ] 22.4 Aggregate and command-test the gate-blocking driver-specific direct recovery and ownership-handback runbooks for pgBackRest, XtraBackup, MariaDB Backup, PBM, ClickHouse, Redis, Valkey, RabbitMQ, MinIO, Meilisearch, NATS, and Restic artifact repositories, covering exact image/helper acquisition, Onebox absence, prerequisite-safe protection disablement before runner/unit/helper removal, repository and previous-volume preservation, and unsupported destructive cleanup; fail consistency if any graduated envelope lacks its already-passing runbook.
- [ ] 22.5 Ensure documentation names a driver/version/recovery envelope as `Managed` only after its independent qualification gate has passed; add a consistency test comparing published claims with the runtime capability catalogue.
- [ ] 22.6 Run formatting, static analysis, unit, conformance, redaction, systemd, native-helper, local-Docker E2E, schema parity, workflow validation, and documentation command suites, recording exact commands and passing evidence in the change.
- [ ] 22.7 Run strict OpenSpec validation and resolve every diagnostic; confirm no proposed behavior is described as currently shipped and no remote backup, service volume, previous volume, or foreign resource gains an implicit deletion path before requesting archive review.
